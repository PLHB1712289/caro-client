import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  container: {
    width: "100%",
    background: "rgba(0,0,0,0.1)",
    display: "flex",
    padding: 10,
    marginTop: 10,
    cursor: "pointer",
  },
  column: {
    textAlign: "center",
  },
}));
