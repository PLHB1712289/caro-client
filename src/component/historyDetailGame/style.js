import { makeStyles } from "@material-ui/core";
export default makeStyles((theme) => ({
  root: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    fontSize: "1rem",
    height: "91vh",
    zIndex: 100,
  },
  board: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
}));
